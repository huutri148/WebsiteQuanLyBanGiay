const styles = (theme) => ({
  title: {
    padding: theme.spacing(4, 0),
    textTransform: "none",
    fontSize: 32,
    color: "darkslateblue",
    fontWeight: "Bold",
  },
  searchInput: {
    display: "flex",
    justifyContent: "center",
  },
  table: {
    padding: theme.spacing(0, 8),
  },
});
export default styles;
